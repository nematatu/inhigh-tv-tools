# インハイTV 試合リンク拡張

Chrome / EdgeのManifest V3拡張です。`../data/matches.json` を元に、対象のBIRD SCOREページへ「動画を見る」を追加し、インハイTV公式アーカイブを開いたときに試合開始位置へ移動します。

データを更新する場合はプロジェクトのルートで `npm run build:data` を実行してから、拡張機能を再読み込みしてください。

既存の `inhigh-tv-skip-extension` とは別の拡張です。
